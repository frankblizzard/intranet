.row
  .span12
    .review_image
      %h2= "#{@review_image.review.name} - #{@review_image.name}"
      .project= "Project: #{link_to @review_image.review.project.name_number, @review_image.review.project}".html_safe
      %br
      %p.alert.alert-info= "Lorem ipsum dolor sit amet, consectetur adipisicing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua. Ut enim ad minim veniam, quis nostrud exercitation ullamco laboris nisi ut aliquip ex ea commodo consequat. Duis aute irure dolor in reprehenderit in voluptate velit esse cillum dolore eu fugiat nulla pariatur. Excepteur sint occaecat cupidatat non proident, sunt in culpa qui officia deserunt mollit anim id est laborum."

.row
  .span8
    .review_image_detail
      .image= image_tag @review_image.photo.url(:medium), :id => "review_image"
      #comments
        - @feedback.with_position.each do |fb|
          .feedback{ :style => "position: absolute; left: #{fb.x1}px; top: #{fb.y1}px; width: #{fb.w}px; height: #{fb.h}px" }= fb.content

    .comments_area
      - @feedback.with_position.each do |fb|
        .user_feedback
          %span.user= fb.user.profile.name
          %span.date= " wrote #{time_ago_in_words(fb.created_at)} ago: "
          %span.message= fb.content

      
    %br.clear
  .span4
    .preview_window= image_tag @review_image.photo.url(:medium), :id => "preview"
    .coords= render :partial => 'coord_form', :locals => {:image_id => @review_image.id}


.backlink= link_to "Go Back", review_path(@review_image.review), :title => 'Back to Project', :class => 'btn' 
%br
.debug= debug @feedback
%br
%br
  